{% extends 'base.twig' %}

{% block title %}
  {{ item.name }}
{% endblock %}

{% block content %}
    {# <div class="container text-center w-75 mx-auto rounded border border-success-subtle shadow p-4">
        <h1 class="title ms-2">{{ item.name }}</h1>
        <p class="ms-3">{{ item.description }}</p>
        <img class="w-25" src="/assets/items/{{ item.image }}" alt="{{ item.name }}" />
        <p>Price: {{ item.price }}</p>
    <div> #}
    <div class="d-flex flex-row">
        <div class="card w-25 rounded border border-success-subtle shadow p-4"> 
            <img class="card-img-top" src="/assets/items/{{ item.image }}" alt="{{ item.name }}" />
        </div>
        <div class="ms-5">  
            <h1 class="title ms-1">{{ item.name }}</h1>
            <p class="ms-3">{{ item.description }}</p>
        </div>
    </div>
    

{% endblock %}